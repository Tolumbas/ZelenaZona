import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	smsButton: {
		backgroundColor: 'red',
		width: 100,
		height: 100,
		position: 'absolute',
		bottom: 0,
		left: 0
	}
});
